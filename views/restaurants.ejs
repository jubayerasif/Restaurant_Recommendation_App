<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head') %>
    <link rel="stylesheet" href="styles/restaurants.css" />
  </head>
  <body>
    <!-- Include header and side drawer partials -->
    <%- include('includes/header') %>
    <%- include('includes/side-drawer') %>

    <main>
      <!-- Recommended restaurants heading -->
      <h1>Recommended restaurants</h1>

      <!-- Check if there are no restaurants -->
      <% if (numberOfRestaurants === 0) { %>
      <!-- Display message for no restaurants -->
      <p>
        Unfortunately, we have no restaurants yet - maybe start recommending
        some?
      </p>
      <% } else { %>
      <!-- Display message for existing restaurants -->
      <p>Find your next favorite restaurants with help of our other users!</p>
      <!-- Display the number of restaurants found -->
      <p>We found <%= numberOfRestaurants %> restaurants.</p>

      <!-- Form to change restaurant order -->
      <form action="/restaurants" method="GET">
        <!-- Hidden input to send next order value -->
        <input type="hidden" value="<%= nextOrder %>" name="order" />
        <!-- Button to change order -->
        <button class="btn">Change Order</button>
      </form>

      <!-- List of restaurants -->
      <ul id="restaurants-list">
        <!-- Loop through each restaurant and include the restaurant item partial -->
        <% for (const restaurant of restaurants) { %>
        <%-include('includes/restaurants/restaurant-item', {restaurant:
        restaurant}) %> <% } %>
      </ul>
      <% } %>
    </main>
  </body>
</html>
